/**
 * config.js — ตั้งค่าที่เดียวของหน้าเว็บ
 *
 * API_URL = URL ของ Apps Script Web App (ลงท้าย /exec)
 * ปล่อยว่าง = โหมดทดลอง ใช้ข้อมูลตัวอย่างในเครื่อง (ไม่แตะชีตจริง)
 * เปิดโหมดทดลองชั่วคราวได้แม้ใส่ URL แล้ว: เติม ?demo=1 ท้ายลิงก์
 */
window.APP_CONFIG = {
  API_URL: '',
};
